{
  "name": "api-public",
  "contributors": [
    "Caio Delgado <caiodelgado.csd@gmail.com>",
    "Italo <italo.i@live.com>"
  ],
  "license": "UNLICENSED",
  "private": true,
  "engines": [
    "node >= 8.9.4",
    "npm >= 5.6.0"
  ],
  "scripts": {
    "lint": "standard --global after --global afterEach --global before --global beforeEach --global describe --global it",
    "test": "mocha test --async-only --check-leaks --full-trace --recursive --throw-deprecation",
    "start:debug": "DEBUG=api-public:* node bin/www",
    "start": "node bin/www"
  },
  "dependencies": {
    "@mantris/appfy": "^0.2.27",
    "axios": "^0.18.0",
    "cors": "^2.8.4",
    "express-rescue": "^1.0.11",
    "sugar-env": "^1.1.0"
  },
  "devDependencies": {
    "axiosist": "^0.1.0",
    "chai": "^4.1.2",
    "lodash.merge": "^4.6.1",
    "mocha": "^5.0.4",
    "nock": "^9.2.3",
    "standard": "^10.0.3"
  }
}
